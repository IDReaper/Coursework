import os, math, numpy
import sys

def function(func):
    f(x)_L = [0]
    x_L = [0]
    xC = -1
    xF = -1
    xadd = 0
    fadd = 0
    var_set = input("Enter count of x vars: ")
    for i in range(var_set):
        xC = xC + 1
        xF = xF + 1
        x = input("Enter val: ")
        f(x) = func
        f(x)_L = [xC] = f
        
        
        
        
    
